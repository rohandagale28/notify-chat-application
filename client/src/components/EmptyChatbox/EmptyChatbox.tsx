export const EmptyChatbox = ({ text }) => {
    return (
        <>
            <div className="chatbox-empty">
                <div className="chatbox-empty-title">
                    {text}
                </div>
            </div>
        </>
    )
}
